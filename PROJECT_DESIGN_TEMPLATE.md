# vi-agent-core Agent Runtime 系统详细设计模板（项目专属・长期复用）

> 用途：用于 `vi-agent-core` 中与 Agent Runtime 相关的功能开发、协议演进、架构重构、存储迁移。  
> 特点：保留仓库固定基线约束，移除“某一次迭代专属答案”，便于后续反复复用。

---

## 0. 使用说明

### 0.1 模板适用范围

- 适用：
  - Runtime 主链路改造
  - `/chat` 或 `/chat/stream` 协议变更
  - 会话模型（conversation/session/turn/message/tool/run）相关改动
  - Provider / Tool / Persistence 边界调整
- 不适用：
  - 纯前端页面与文案变更
  - 与 Runtime 无关的独立业务模块改造

### 0.2 填写规则

- `[必须填写]`：提交评审前必须补全。
- `[按需填写]`：仅在本次变更涉及时填写；不涉及请写 `N/A`。
- `[项目固定基线]`：来自仓库文档的长期约束，原则上不改；若确需变更，必须先更新根文档。

---

## 1. 文档元信息

- 文档名称：`[必须填写]`
- 变更主题：`[必须填写]`
- 目标分支 / 迭代：`[必须填写]`
- 文档版本：`v[必须填写]`
- 状态：`Draft / Review / Confirmed / Frozen`
- 作者：`[必须填写]`
- 评审人：`[必须填写]`
- 关联文档：
  - `AGENTS.md`
  - `ARCHITECTURE.md`
  - `CODE_REVIEW.md`
  - `PROJECT_PLAN.md`
  - `[按需填写：本次相关专题设计文档]`

---

## 2. 变更摘要（Executive Summary）

### 2.1 一句话摘要

`[必须填写：本次改什么、为什么改]`

### 2.2 本次范围

1. `[必须填写：范围 1]`
2. `[必须填写：范围 2]`
3. `[必须填写：范围 3]`

### 2.3 明确不做

1. `[必须填写：不做 1]`
2. `[必须填写：不做 2]`

---

## 3. 背景与问题定义

### 3.1 当前现状（仅写与本次直接相关）

- `[必须填写]`
- `[必须填写]`

### 3.2 已确认问题

1. `[必须填写]`
2. `[必须填写]`
3. `[按需填写]`

### 3.3 不改风险

- `[必须填写：如果不做，会有什么后果]`

---

## 4. 项目固定基线（不可绕开）

### 4.1 模块与依赖方向（项目固定基线）

- `vi-agent-core-common`
- `vi-agent-core-model -> common`
- `vi-agent-core-runtime -> model + common`
- `vi-agent-core-infra -> model + common`
- `vi-agent-core-app -> runtime + infra + model + common`

### 4.2 分层职责（项目固定基线）

- `app`：controller / application service / Spring 装配 / 协议适配
- `runtime`：主链路编排、loop、上下文装配、工具协调
- `infra`：provider、DB/Redis repository、外部适配实现
- `model`：领域模型、值对象、port 契约
- `common`：异常、ID、通用无状态工具

### 4.3 代码治理红线（项目固定基线）

- 禁止 `runtime -> infra` 反向依赖
- 禁止 mapper / dao / repository 混在同一文件
- 禁止 `model.port` 放 DTO/Command/Record
- 禁止为兼容旧测试保留已确认过时逻辑

---

## 5. 术语、语义与标识

### 5.1 术语表（按需裁剪）

| 术语 | 定义 | 本次是否涉及 |
|---|---|---|
| `conversation` | 前端会话窗口 | `Y/N` |
| `session` | 会话窗口下的一段运行时生命周期 | `Y/N` |
| `turn` | 一次请求执行单元 | `Y/N` |
| `message` | 对话事实记录 | `Y/N` |
| `tool_call` | 工具调用事实 | `Y/N` |
| `tool_result` | 工具执行结果事实 | `Y/N` |
| `run` | 运行实体及状态 | `Y/N` |

### 5.2 标识符定义

| 标识符 | 用途 | 生成方 | 是否对外返回 |
|---|---|---|---|
| `requestId` | 幂等键 | `[必须填写]` | `[必须填写]` |
| `conversationId` | 会话窗口标识 | `[必须填写]` | `[必须填写]` |
| `sessionId` | 生命周期标识 | `[必须填写]` | `[必须填写]` |
| `turnId` | 单次执行标识 | `[必须填写]` | `[必须填写]` |
| `runId` | 运行标识 | `[必须填写]` | `[必须填写]` |
| `messageId` | 消息标识 | `[必须填写]` | `[必须填写]` |
| `traceId` | 内部观测链路 | 后端 | 否（仅日志/MDC） |

### 5.3 显式语义枚举（按需）

- `SessionMode`：`[按需填写或 N/A]`
- `RunStatus`：`[按需填写或 N/A]`
- `StreamEventType`：`[按需填写或 N/A]`
- `FinishReason`：`[按需填写或 N/A]`

---

## 6. 对外协议设计（API / Stream）

### 6.1 请求契约

```java
class [RequestDtoName] {
    [Type] [field];
}
```

字段表：

| 字段 | 类型 | 必填 | 含义 | 校验规则 |
|---|---|---|---|---|
| `[field]` | `[type]` | `Y/N` | `[必须填写]` | `[必须填写]` |

### 6.2 同步响应契约

```java
class [ResponseDtoName] {
    [Type] [field];
}
```

### 6.3 流式事件契约（按需）

```java
class [StreamEventDtoName] {
    [Type] [field];
}
```

### 6.4 协议红线

- `traceId` 不得出现在对外 DTO。
- 语义不得依赖“id 是否为空”的隐式推断（应显式枚举/字段表达）。
- `requestId` 命中幂等时，同步与流式语义必须一致。

---

## 7. 领域模型设计

### 7.1 领域对象清单

| 对象 | 职责 | 所在模块/包 | 本次变更类型（新增/修改/删除） |
|---|---|---|---|
| `[DomainObject]` | `[必须填写]` | `[必须填写]` | `[必须填写]` |

### 7.2 关键值对象 / 命令对象

- `[按需填写]`

### 7.3 领域规则与状态流转

1. `[必须填写：规则 1]`
2. `[必须填写：规则 2]`
3. `[按需填写：规则 3]`

---

## 8. 持久化与数据设计（按需）

### 8.1 事实源与缓存边界

- 事实源：`[必须填写]`
- 缓存层职责：`[必须填写]`
- miss 回填策略：`[必须填写]`

### 8.2 关系型存储设计（若涉及）

| 表名 | 用途 | 主键/唯一约束 | 关键索引 | 本次动作 |
|---|---|---|---|---|
| `[table]` | `[必须填写]` | `[必须填写]` | `[必须填写]` | `[新增/修改/删除/N/A]` |

### 8.3 Redis / 文档结构（若涉及）

| 文档或 Key 模式 | 用途 | TTL | miss 处理 |
|---|---|---|---|
| `[keyPattern]` | `[必须填写]` | `[必须填写]` | `[必须填写]` |

### 8.4 迁移策略（若涉及）

- 迁移机制：`Flyway / [其他]`
- 脚本命名：`V{version}__{description}.sql`
- 回滚策略：`[必须填写]`

---

## 9. Runtime 主链路设计

### 9.1 角色职责分配

| 组件 | 职责 | 本次变更 |
|---|---|---|
| `RuntimeOrchestrator` | run-level orchestration | `[必须填写]` |
| `AgentLoopEngine` | llm-tool loop owner | `[必须填写]` |
| `SessionResolutionService` | 会话解析与创建 | `[按需填写]` |
| `TurnLifecycleService` | turn 生命周期 | `[按需填写]` |
| `PersistenceCoordinator` | 持久化协调 | `[按需填写]` |

### 9.2 主链路时序（同步）

```text
[Controller]
-> [ApplicationService]
-> [RuntimeOrchestrator]
   -> [协作者...]
-> [ResponseAssembler]
-> [Response]
```

### 9.3 主链路时序（流式，按需）

```text
[StreamController]
-> [StreamApplicationService]
-> [RuntimeOrchestrator]
   -> [LoopEngine + EventEmitter]
-> [SSE Event Stream]
```

---

## 10. 幂等、并发与状态机（按需）

### 10.1 幂等策略

| 命中场景 | 处理策略 | 返回语义 |
|---|---|---|
| `[COMPLETED]` | `[必须填写]` | `[必须填写]` |
| `[RUNNING]` | `[必须填写]` | `[必须填写]` |
| `[FAILED]` | `[必须填写]` | `[必须填写]` |

### 10.2 并发策略

- 并发范围：`[必须填写：按 session / conversation / request 等]`
- 控制策略：`[拒绝/排队/覆盖]`
- 同步与流式一致性：`[必须填写]`

### 10.3 状态机

```text
[STATE_A] -> [STATE_B] -> [STATE_C]
```

---

## 11. Provider 与 Tool 设计（按需）

### 11.1 模型调用端口

- 统一端口：`[必须填写：如 LlmGateway]`
- 返回对象：`[必须填写：如 ModelResponse]`
- Provider-neutral 字段：`[必须填写]`

### 11.2 Provider 选择策略

- 当前默认 provider：`[按环境填写]`
- 备选 provider：`[按需填写]`
- 路由策略：`[单 provider / 静态切换 / 动态路由 / N/A]`

### 11.3 Tool 协议

- tool call 记录策略：`[必须填写]`
- tool result 记录策略：`[必须填写]`
- 异常工具处理：`[必须填写]`

---

## 12. 配置设计（按需）

| 配置项 | 默认值 | 是否必填 | 说明 |
|---|---|---|---|
| `[config.key]` | `[value]` | `Y/N` | `[必须填写]` |

配置校验规则：

- `[必须填写：空值、非法值、冲突值处理]`

---

## 13. 可测试性与测试计划

### 13.1 必测场景

1. `[必须填写]`
2. `[必须填写]`
3. `[按需填写]`

### 13.2 测试分层

- 单元测试：`[必须填写]`
- 集成测试：`[必须填写]`
- 回归测试：`[按需填写]`

### 13.3 与旧测试冲突处理

- 若旧测试与新语义冲突：`更新或删除，不保留过时代码`。
- 本次需删除/替换测试清单：`[必须填写或 N/A]`

---

## 14. 分阶段实施计划

| 阶段 | 目标 | 改动范围 | 完成标准 | 回滚点 |
|---|---|---|---|---|
| 阶段 1 | `[必须填写]` | `[必须填写]` | `[必须填写]` | `[必须填写]` |
| 阶段 2 | `[必须填写]` | `[必须填写]` | `[必须填写]` | `[必须填写]` |
| 阶段 3 | `[按需填写]` | `[按需填写]` | `[按需填写]` | `[按需填写]` |

---

## 15. 风险与回滚

### 15.1 风险清单

| 风险 | 影响 | 概率 | 应对方案 | Owner |
|---|---|---|---|---|
| `[必须填写]` | `[必须填写]` | `高/中/低` | `[必须填写]` | `[必须填写]` |

### 15.2 回滚策略

- 协议层回滚：`[必须填写或 N/A]`
- 数据层回滚：`[必须填写或 N/A]`
- 主链路回滚：`[必须填写或 N/A]`

---

## 16. 验收标准

### 16.1 功能验收

- `[必须填写]`

### 16.2 架构与分层验收

- 不破坏模块依赖方向
- 关键职责按设计落位
- 无明显跨层绕过

### 16.3 协议与数据验收

- 协议字段、语义与设计一致
- 数据结构与迁移脚本可执行
- 缓存回填策略可验证

### 16.4 测试验收

- 必测场景全部通过
- 过时测试已清理或重建

---

## 17. 最终交付清单

1. 代码改造清单（类/包级别）
2. 配置改造清单
3. 数据迁移脚本清单（若有）
4. 测试新增/修改/删除清单
5. 变更总结（主链路变化、废弃路径、未做项）

---

## 18. 给实现代理的执行指令模板（可直接复用）

```text
你现在是本仓库实现代理。先完整阅读并严格遵守：
1. 根目录 AGENTS.md
2. 根目录 PROJECT_PLAN.md
3. 根目录 ARCHITECTURE.md
4. 根目录 CODE_REVIEW.md
5. execution-phase/README.md
6. 当前阶段 README.md
7. 当前阶段 design.md
8. 当前阶段 plan.md
9. 当前阶段 test.md
10. 与当前任务相关的历史强契约文档
11. 相关模块 AGENTS.md
12. 相关源码与测试

执行要求：
- 严格按本文档“分阶段实施计划”执行，不跳阶段
- 若旧测试不符合新架构，直接同步更新或删除
- 不做与本次范围无关的额外架构扩展
- 最终交付：代码、配置、迁移脚本、测试、变更总结
```

---

## 19. 文档维护规则

- 本模板是 `vi-agent-core` 的 Agent Runtime 专属复用模板。
- 采用增量更新，不做无理由整体改写。
- 若仓库基线发生变化，先更新根文档，再同步更新本模板。
- 新设计点必须同步落到：术语、契约、领域、存储、主链路、测试、阶段计划。

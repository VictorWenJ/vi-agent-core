spring:
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      database: ${REDIS_DB:1}

logging:
  level:
    root: WARN
    com.vi.agent.core: INFO

vi:
  agent:
    provider:
      deepseek:
        api-key: test-key

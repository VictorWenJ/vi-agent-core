spring:
  datasource:
    url: jdbc:h2:mem:vi_agent_core_dev;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true

logging:
  level:
    com.vi.agent.core: DEBUG
